{
  "name": "@repo/oci-layout",
  "version": "0.0.0",
  "private": true,
  "license": "GPL-2.0-only",
  "type": "module",
  "scripts": {
    "build": "go build -v -o out/layoci .",
    "format": "go fmt .",
    "format:check": "gofmt -l . | { read x && echo \"$x\"; cat; exit 1; }\n"
  },
  "bin": {
    "layoci": "./out/layoci"
  }
}
